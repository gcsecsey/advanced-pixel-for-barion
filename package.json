{
  "name": "advanced-pixel-for-barion",
  "private": true,
  "description": "Tooling for the consent browser suite only. The unit tests stay dependency-free and run under `node --test` and `php tests/<file>.php`. Not shipped — see .distignore.",
  "license": "GPL-2.0-or-later",
  "scripts": {
    "test:browser": "node tests/playground/run.mjs"
  },
  "devDependencies": {
    "@wp-playground/cli": "3.1.51",
    "playwright": "1.62.1"
  }
}
